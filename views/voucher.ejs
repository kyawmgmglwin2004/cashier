<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Market Cashier Voucher</title>
    <link rel="stylesheet" href="/styles.css">
    <style>
        :root {
            --primary-color: #4CAF50;
            --secondary-color: #f4f4f4;
            --text-color: #333;
            --border-color: #ddd;
            --shadow-color: rgba(0, 0, 0, 0.1);
            --font-family: 'Arial', sans-serif;
        }

        body {
            font-family: var(--font-family);
            max-width: 400px;
            margin: 20px auto;
            text-align: center;
            background-color: var(--secondary-color);
            color: var(--text-color);
        }

        .voucher {
            padding: 20px;
            border: 1px solid var(--border-color);
            border-radius: 10px;
            box-shadow: 0 4px 6px var(--shadow-color);
            background-color: white;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 10px;
        }

        th, td {
            border: 1px solid var(--border-color);
            padding: 10px;
            text-align: left;
            border-radius: 5px;
        }

        th {
            background-color: var(--primary-color);
            color: white;
        }

        .total {
            font-weight: bold;
            margin-top: 10px;
            font-size: 1.2em;
        }

        .print-btn {
            margin-top: 20px;
            padding: 10px 20px;
            background: var(--primary-color);
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 1em;
        }

        .print-btn:hover {
            background: #45a049;
        }
    </style>
</head>
<body>
    <div class="voucher">
        <h2>City Market Cashier Voucher</h2>
        <p>Date: <%= new Date().toLocaleDateString() %></p>
        <p>Time: <%= new Date().toLocaleTimeString() %></p>

        <table>
            <tr>
                <th>Item</th>
                <th>Price</th>
            </tr>
            <% voucherData.items.forEach(item => { %>
            <tr>
                <td><%= item.name %></td>
                <td><%= item.price %> Ks</td>
            </tr>
            <% }); %>
        </table>

        <h3 class="total">Total Amount: <%= voucherData.totalAmount %> Ks</h3>

        <h4>Thank You</h4>

        <button class="print-btn" onclick="window.print()">Print Voucher</button>
    </div>

    <script src="/js/front.js"></script>
</body>
</html>
